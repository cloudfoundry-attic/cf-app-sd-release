---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: '1.9'

inputs:
  - name: service-discovery-release

params: {}

run:
  path: service-discovery-release/ci/tasks/test-unit

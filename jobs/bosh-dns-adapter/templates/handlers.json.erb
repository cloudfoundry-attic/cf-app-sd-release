 <%=

config = [
  {
    "domain" => "sd-local.",
    "cache" => {"enabled" => false},
    "source" => {
      "type" => "http",
      "recursors" => ["#{p("address")}:#{p("port")}"]
    }
  }
]

require 'json'
JSON.dump(config)
%>

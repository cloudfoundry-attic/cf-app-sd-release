<%=
require 'json'
config = {
    "address" => p('address'),
    "port" => "#{p('port')}",
    "service_discovery_controller_address" => "#{link('service-discovery-controller').p('address')}",
    "service_discovery_controller_port" => "#{link('service-discovery-controller').p('port')}"
}

JSON.dump(config)
%>